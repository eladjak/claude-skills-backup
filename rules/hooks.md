# Hooks

## Active Hooks (in ~/.claude/settings.json)
- **PreToolUse**: tmux reminder for long commands, git push review, doc blocker
- **PostToolUse**: PR logging, Prettier auto-format, TypeScript check, console.log warning
- **Stop**: console.log audit on modified files

## TodoWrite: Use for multi-step tasks to track progress and enable real-time steering.
